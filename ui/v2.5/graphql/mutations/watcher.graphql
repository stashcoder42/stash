mutation EnableWatcher {
  enableWatcher
}

mutation DisableWatcher {
  disableWatcher
}

mutation ConfigureWatcher($input: ConfigWatcherInput!) {
  configureWatcher(input: $input) {
    enabled
    debounceMs
    scanOnChange
    identifyOnChange
    cleanOnRemove
  }
}
